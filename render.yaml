services:
  - type: web
    name: color-by-number
    runtime: docker
    plan: free
    envVars:
      - key: PORT
        value: "10000"
      - key: GRADIO_ANALYTICS_ENABLED
        value: "False"
    healthCheckPath: /
    autoDeploy: true
