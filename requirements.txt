# Used by Hugging Face Spaces and Render/Fly/Railway deployments.
# opencv-python-headless has no GUI deps (libGL, libSM, ...) so it works
# on stock server images. The Python API is identical to opencv-python.
opencv-python-headless==4.6.0.66
scipy==1.11.4
python-polylabel==0.6
# Gradio 5.x is required: 4.44.0 breaks against current pydantic/starlette/jinja2
# releases (gradio_client schema bug, jinja2 cache key crash, etc.).
gradio==5.49.1
numpy<2
